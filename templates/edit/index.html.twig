{% extends 'base.html.twig' %}

{% block title %}Modifer un produit{% endblock %}

{% block body %}

    <div class="container col-lg-4 FormAdd">
        {# J'affiche mon formulaire #}
        <h1>Editer {{ OneProduct.name }}</h1>

        {{ form_start(AddProductFormType) }} <!-- <form method="POST>">-->

        <div >
            {{ form_label(AddProductFormType.name) }}
            {{ form_widget(AddProductFormType.name) }}
        </div>



        {{ form_widget(AddProductFormType) }}<!-- input-->
        <div class="EditImage">
            <img class="img-fluid" src="{{ asset('IMG/uploads/' ~ OneProduct.image) }}" alt="">
        </div>

        <button class="btn btn-primary">Modifier</button>
        {{ form_end(AddProductFormType) }}<!-- </form>>-->
    </div>
{% endblock %}
